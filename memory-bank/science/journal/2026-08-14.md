# Journal Science — 2026-08-14 (soir)

## Plan P2 — Dénormalisation Redis

**Agent** : PATCHER
**Objectif** : `getRecentCampaigns()` passe de 40 Mo à ~4 Ko via clés `:meta`

### Modifications

1. **`backend/app/core/upstash.py`** — `save_campaign()` :
   - Pipeline passé de 4 à 8 commandes
   - 3 nouvelles clés : `:meta` (6 champs, ~200 o, TTL 30j), `:tirages` (bruts seuls, TTL 7j)
   - Payload complet inchangé (rétrocompatible)
   - `.get()` en cascade pour COP/STR → None si absents

2. **`backend/tests/test_persistance.py`** :
   - `test_save_campaign_pipeline_commands` adapté (8 commandes, unpacking 8 variables)
   - Mock `return_value` corrigé (`["OK"] * 8`)
   - Nouvelle classe `TestSaveCampaignDenormalized` (6 tests) :
     - `test_meta_key_written_with_six_fields`
     - `test_tirages_key_written_separately`
     - `test_tirages_ttl_is_7_days`
     - `test_meta_ttl_is_30_days`
     - `test_meta_handles_missing_statistiques_gracefully`
     - `test_full_payload_still_has_tirages`

### Résultats

- **Tests** : 43/43 verts (17 persistance + 26 autres)
- **Invariant A1** : Δh_gen = 2520.807 kJ/kg (OK, attendu 2520.874 ± 0.5)
- **Zéro régression** : aucun test cassé, aucune physique touchée

### Escalade SUPERMAN/BUILDER (frontend — INTERDIT à PATCHER)

Le backend écrit maintenant les clés `:meta`. Le frontend doit :
1. `getRecentCampaigns()` : lire `:meta` au lieu du payload complet
2. `clearCircuit()` : nettoyer aussi `:meta` et `:tirages`
3. Fallback rétrocompatible si `:meta` n'existe pas (vieilles campagnes)
